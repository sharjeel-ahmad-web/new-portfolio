import { HeroContent } from "@/components/ui/HeroContent";

export const Hero = () => {
  return (
    <div className="flex flex-col justify-center items-center h-screen w-full bg-dark">
      <HeroContent />
    </div>
  );
};
